{
  "name": "Kotka-PBS",
  "author": "<akauppi@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "npx vuepress dev docs --host localhost",
    "build": "npx vuepress build docs",
    "_serve": "static-server -p 3101 --follow-symlink _fakeRoot",
    "build-and-serve": "npm run build && npm run _serve",
    "predeploy": "npm run build",
    "deploy": "sh/deploy.sh",
    "_clean": "rm -rf dist/*",
    "_vp:dev": "npx vitepress dev docs"
  },
  "//scripts": {
    "_": "Targets starting '_' are undocumented (not mentioned in 'README'). You can rename or remove them, easier.",
    "_vp:dev": "Secondary tool; not in feature parity yet (Jul 2020) with VuePress, but once is, consider to change."
  },
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.5.2",
    "markdown-it-footnote": "^3.0.2",
    "static-server": "^2.2.1",
    "vuepress": "^1.5.2",
    "vitepress": "^0.4.1"
  },
  "//devDependencies": {
    "watchpack": [
      "1.6.1 only needed until this is fixed -> https://github.com/vuejs/vuepress/issues/2392"
    ]
  },
  "//": {
    "type": [
      "Cannot have 'type': 'module' since VuePress requires 'config.js' with extension (likely); did not find '.cjs'."
    ]
  }
}
